# G2 multi-root monitor terminal run and three-root successor — 2026-08-29

Hosted run `33240864641-1`, bound to source commit `a52515ca8c5004c950551a5d8fb5f761068e3210`, completed the California, CNJ DataJud documentation and British Columbia sitemap requests. It sealed 1,212 observations before the FCFCOA request timed out. The run therefore terminated with exit code 2 and did not evaluate candidate eligibility.

The partial exposure ledger and self-binding terminal receipt are preserved as immutable evidence. No returned locator was opened.

The successor removes FCFCOA from active monitoring because its frozen sitemap exposed only the site homepage and the hosted request consumed the entire request timeout. The successor is a distinct three-root campaign, not a repair of the failed run. It binds both the original baseline and failed-run partial ledger as cumulative exposure and retains all existing zero-retry, partial-sealing and action-required controls.

This disposition does not establish candidate eligibility, extraction reproducibility, G2 acceptance, rights clearance, publication or release.
