# G2 preflight-contract unblock grouped decision packet — 2026-08-26

## Decision needed

The authorized physical-isolation lineage stopped before source access because
the extractor invoked a two-argument verifier with one argument. The repository
now provides and tests an unambiguous one-path CLI. The failed packet remains
immutable and cannot be resumed.

### Option A — recommended: one final CLI-bound isolated lineage

Authorize a new prospectively frozen lineage using the same four exact source
editions and freshly built allowlist-only role workspaces. Each extractor must
run the exact frozen CLI command against its own workspace before source access.
Two fresh role-separated extractors, one network-disabled comparator and one
advisory review are permitted. Existing thresholds and terminal rules remain
unchanged.

Trade-off: this is the smallest credible remediation and introduces no new
source exposure, but it consumes another lineage and may still terminate on a
semantic discrepancy. Contingency: any failure stops without another automatic
retry; preserve G2 at 9/13.

### Option B: stop further extraction attempts

Retain the acquired editions and both terminal lineages as preparation and
failure evidence. G2 remains blocked. This avoids further execution cost but
cannot satisfy C04/C07.

## Recommended owner wording

> I approve Option A in the G2 preflight-contract unblock grouped decision
> packet dated 2026-08-26. I authorize one new prospectively frozen lineage over
> the same four exact editions using freshly built explicit-allowlist role
> workspaces and the exact one-path workspace-verification CLI frozen in the
> packet. I authorize two fresh artifact-isolated extraction agents, one
> network-disabled exact comparator and role-separated advisory review.
>
> Critical concordance remains 100 percent and overall populated-field
> concordance at least 99 percent. No fuzzy matching, waiver, output repair,
> failed-output reuse, result-driven substitution or automatic rerun is
> permitted. Any scope, digest, prohibited-data, isolation, schema, seal,
> critical-difference or comparator-contract failure terminates the lineage.
>
> This authorizes no network access, new source acquisition, contact, rights
> clearance, gold promotion, publication, release, maturity promotion or G2
> passage. A passing result and completed maturity matrix must return to me for
> one grouped accountable adjudication.

