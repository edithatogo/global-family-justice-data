# G2 successor overlap stop — owner decision packet — 2026-08-16

Status: awaiting sole-owner decision. The execution has already stopped. This
packet authorizes nothing and cannot pass G2.

## Decision required

The authorized 208-query successor completed its network calls within the
frozen boundary, then failed semantic verification because passive results
overlapped prior exposure. The full predecessor-chain audit identifies 23
overlapping URLs and treats all 609 observed URLs as exposed.

### Option A — accept the stop and prepare a new prospective successor

Recommended. Preserve the entire run as terminal failed evidence. Authorize
repository-only preparation of a separately identified design that adds all
609 observed URLs to the cumulative denylist and binds complete predecessor-
chain traversal before execution. The finished design must return for a new
exact owner decision before any query or URL request.

Trade-offs: retains a path to future unseen evidence but increases design and
review cost. It cannot reuse, filter or promote any current result.

Contingency: if a defensible unseen frame cannot be frozen without weakening
the existing thresholds or exposure rules, stop and choose Option B.

### Option B — terminate the metadata-search route

Preserve the evidence and seek a different prospective discovery method.

Trade-offs: avoids repeated calibration against a progressively contaminated
universe but delays blind-holdout evidence and requires a new methods design.

Contingency: no further G2 discovery execution occurs until a different
digest-bound packet is approved.

### Option C — accept exposure evidence only and defer

Record the run solely as evidence that the boundary controls stopped correctly,
without authorizing a successor or abandoning the method.

Trade-offs: lowest immediate cost, but `WI-G2-07` and G2 remain blocked with no
active evidence route.

Contingency: any later restart requires a new packet based on the then-current
cumulative exposure chain.

## Recommendation and rationale

Choose Option A. The network boundary and deterministic stop worked as
intended, while the cumulative-chain audit exposed a repository-owned control
gap that is now addressable prospectively. A new lineage can preserve the
non-waivable concordance and no-retrospective-repair rules. The current lineage
must remain failed.

## Exact approval wording

> I approve Option A in the G2 successor overlap-stop owner decision packet
> dated 2026-08-16. I accept the 208-query successor execution as immutable
> terminal failed evidence because its passive results overlap the cumulative
> predecessor exposure chain. No current result or subset may be repaired,
> filtered, promoted or treated as unseen.
>
> I authorize repository-only preparation of a separately identified
> successor design that adds all 609 observed URLs to the cumulative exposure
> denylist and uses digest-bound complete-chain traversal. The completed design
> must return to me with exact scope, query manifest, controls, stopping rules,
> resource estimate, signed freeze and proposed authorization wording before
> any network or source access.
>
> This decision does not authorize queries, URL or source access, extraction,
> contact, rights acceptance, publication, release or G2 passage.
