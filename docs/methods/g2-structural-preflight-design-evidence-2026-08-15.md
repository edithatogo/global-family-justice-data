# G2 structural-preflight design evidence — 2026-08-15

Status: in review; source access and execution not authorized

The sole owner accepted the preceding metadata intake as a valid fail-closed
stop and authorized repository-owned successor-design preparation. The
repository produced a deterministic 44-candidate metadata frame, an 11-entry
exact-PDF acquisition proposal, a separate 33-entry HTML metadata-resolution
proposal, exact structural and generic extraction contracts,
resource bounds, three role-separated advisory reports, a reproduction receipt
and a detached design manifest. All artifacts are bound by
`data/methods/g2/G2HOLDOUT-STRUCTURAL-PREFLIGHT-20260815-01/DESIGN_MANIFEST.sha256`
(SHA-256
`04bcfdeb41a590a3a510b541987c9a1c35e9416dc27db64fdbe9b433fe31ce93`).

The builder and verifier perform no network or source-content operation. The
frame is the complete safe projection of already archived metadata and remains
materially smaller than the panel-recommended 96-candidate design. The security
panel also identified a critical pre-execution acquisition-control gap.

The hardened verifier recomputes the frame, both proposal manifests and receipt,
enforces the exact detached artifact set, validates every panel evidence digest,
and binds the builder, exposure logic, selector, comparator, schemas, contracts
and adversarial tests. Structural eligibility is schema-conditional and
independently classified; deterministic selection and generic comparison have
positive and fail-closed mutation tests.

This evidence establishes only that a source-access-disabled successor design
has been prepared reproducibly. It does not establish structural eligibility,
rights, privacy, security, source integrity, extraction reproducibility, G2
acceptance, publication or release.
