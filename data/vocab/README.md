# Controlled vocabularies

The v1 public contracts use stable identifiers and controlled values defined in JSON Schemas and dictionaries. CSV vocabularies may be added for user-facing labels and translations, but the schemas remain authoritative for structural validation.

Changes during 1.x must follow `docs/standards/versioning-and-deprecation.md`.
