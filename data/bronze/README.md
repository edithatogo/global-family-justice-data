# Bronze layer

Bronze tables are source-native extracts. They preserve original labels, language, units, period structure, footnotes, and source row/column meaning.

Permitted changes are limited to controlled structural extraction, character encoding normalisation, and documented transcription correction. Each table must reference its source version and extraction method. Bronze inputs are immutable within a release and are never edited to resemble the harmonised model.
