# Offline federation draft with one replay attachment

Only the selected scoped object's byte identity is bound to recomputed provenance. Other objects remain pending. This does not establish semantic identity, ownership, factual provenance, custody, rights, standards conformance, publication or acceptance. No source payload or input metadata is copied. Existing helper implementation fingerprint reads and adapter/PROV compiler fingerprint reads occur; no source loader or network request occurs.

Parquet and partner reports are declaration/reference checks only. Known non-Parquet input contradictions are rejected; unknown payload formats and digests remain unverified. No partner ownership or layer equivalence is conferred. Component compiler fingerprint reads are performed.
