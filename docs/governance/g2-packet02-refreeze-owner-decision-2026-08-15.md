# G2 packet 02 refreeze owner decision — 2026-08-15

Decision ID: `D-G2-PACKET02-REFREEZE-20260815`

Decision-maker: repository owner, founder and sole accountable
decision-maker.

Advisory decision packet:
`docs/governance/g2-formal-rerun-owner-decision-packet-2026-08-15.md`.

Advisory decision packet SHA-256:
`abb337a6cd50d37e7a7176dff895a8555d2441b120b8290e65441b376a7b30d2`.

## Owner decision

> I approve superseding G2 packet 01 with a fresh digest-bound packet using the
> current hardened comparator and an atomic semantic extraction contract. The
> approved thresholds remain 100% for every critical fact, exact source excerpt
> and canonical semantic facet, and at least 99% across all populated fields. No
> fuzzy matching or critical waiver is permitted.
>
> I direct both extraction paths to be rerun from the exact source editions.
> Dates are explicit-only for this rerun: an ISO date may be populated only when
> the packet-bound source establishes it; otherwise the source period label is
> preserved and the date remains null.
>
> AUS and ZAF remain in the four-row evidence exercise but under hard methods
> quarantine. Their source contradictions must be captured exactly and must not
> be resolved by inference, promoted, compared or published. BRA remains a
> partial-year protective-measures snapshot, and USA-MN remains a source-defined
> statewide Family clearance statistic. All four rows remain quarantined until
> the fresh comparator and owner adjudication pass.
>
> I approve controlled local-private retention and processing of the four exact
> editions and sealed evidence artifacts. The current public boundary is
> metadata and citation only. No source bytes, screenshots, layouts, substantial
> extracts, sealed outputs, outbound contact, publication, G2 passage or release
> is authorized by this decision. Any later exact-edition reuse or enquiry
> requires a separate digest-bound owner decision.

## Binding implementation

- Packet 01 and its failed diagnostic result remain immutable historical
  evidence and are not edited or promoted.
- Packet 02 must bind the exact source editions, current schemas, comparator,
  atomic contract, instructions and owner decisions before either extractor
  starts.
- Exact source excerpts and canonical atomic semantic facets replace
  unrestricted narrative equality for critical comparison. This does not
  reduce or fuzzy-match a critical field.
- Both formal outputs must be separately produced, schema-valid and
  digest-bound. Extractor-specific identifiers are excluded from concordance.
- `period_start` and `period_end` remain `null` unless the exact edition states
  the ISO date. A source period label is always preserved.
- AUS and ZAF must carry `hard_quarantine`; BRA and USA-MN must carry
  `quarantine`. Concordance cannot remove those dispositions.
- Full source bytes, extraction outputs, differences and panel reports remain
  ignored local-private evidence. Committed records contain bounded metadata,
  statuses and hashes only.
- No outbound contact, publication, gate promotion or release is authorized.

Immutable binding: the signed Git commit containing this decision. Packet 02
must record that exact commit and the decision-file digest.
