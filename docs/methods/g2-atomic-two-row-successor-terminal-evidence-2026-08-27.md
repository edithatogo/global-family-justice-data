# G2 atomic two-row successor terminal evidence — 2026-08-27

The authorized successor `G2PKT-ATOMIC-TWO-ROW-BLIND-20260827-02` was frozen
before delegation using programmatically derived inputs and independent
workspace recomputation. Both fresh role-separated extractors completed in
verified allowlist-only, network-disabled workspaces. Their two-row outputs
passed the frozen JSON schema and atomic cross-field validation and were sealed
before exact comparison.

The comparator produced a terminal failure:

- matched rows: 2; unmatched rows: 0;
- critical concordance: 74/103 (71.8447%); required 100%;
- populated-field concordance: 63/92 (68.4783%); required at least 99%;
- critical differences: 29.

The role-separated advisory review grouped the differences into semantic
classification and scope (14), temporal and cohort interpretation (4),
ambiguity characterization (3), locator interpretation (3), coverage evidence
(2), component taxonomy (2) and quarantine disposition (1). Exact agreement on
both values, units and source-edition identities is insufficient under the
precommitted thresholds.

No output was repaired or reused, no discrepancy was waived or fuzzily
matched, and no automatic rerun occurred. Private sealed artifacts remain
outside Git; their SHA-256 bindings are recorded in `terminal-result.json`.

The advisory recommendation is to adjudicate source-independent semantic,
locator, component and quarantine conventions prospectively before deciding
whether to authorize a wholly new calibration or evidence lineage. The advice
is not independent assurance or an accountable decision.

Conductor remains fail-closed: `G2-C04`, `G2-C07`, `WI-G2-04` and `WI-G2-07`
remain `in_review`, M06 remains L1 and G2 remains blocked. This is immutable
failed evidence, not G2 acceptance, maturity promotion, publication or release.
